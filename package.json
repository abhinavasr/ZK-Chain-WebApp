{
  "name": "zk-chain-funded-contract-poc",
  "version": "3.0.0",
  "private": true,
  "description": "Privacy-preserving Mastercard → PSP → Business → Agent → Merchant funded-contract and settlement POC",
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --host 0.0.0.0",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview -- --host 0.0.0.0",
    "simulate": "node simulation/simulate-flow.js",
    "test": "npm run simulate && npm run build"
  },
  "devDependencies": {}
}
